# Code ownership for jackemouna/alien.
#
# This fork is single-maintainer for now. Every file is owned by @jackemouna.
# When the team grows, scope this file to specific paths so reviews are
# routed correctly (last-match-wins per GitHub semantics).

* @jackemouna
