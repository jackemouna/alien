<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_ALIEN"
        app:queryPatterns="@array/ask_alien_query_patterns">
        <intent
            android:action="ai.alien.app.action.ASK_ALIEN"
            android:targetPackage="ai.alien.app"
            android:targetClass="ai.alien.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
