import Foundation
import AlienKit

// Prefer the AlienKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = AlienKit.AnyCodable
typealias InstanceIdentity = AlienKit.InstanceIdentity
