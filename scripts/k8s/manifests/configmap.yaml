apiVersion: v1
kind: ConfigMap
metadata:
  name: alien-config
  labels:
    app: alien
data:
  alien.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.alien/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Alien Assistant",
            "workspace": "~/.alien/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Alien Assistant

    You are a helpful AI assistant running in Kubernetes.
